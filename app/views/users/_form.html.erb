<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :your_name %><br>
    <%= f.text_field :your_name %>
  </div>
  <div class="field">
    <%= f.label :your_email %><br>
    <%= f.text_field :your_email %>
  </div>
  <div class="field">
    <%= f.label :recipient_name %><br>
    <%= f.text_field :recipient_name %>
  </div>
  <!-- need to add link here that sutopopulates -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
